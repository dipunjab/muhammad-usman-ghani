<div id="ProductQuickView" class="custom-qv-modal">
  <div class="qv-overlay" data-close-modal></div>
  <div class="qv-content">
    <button class="qv-close" data-close-modal>&times;</button>
    <div id="AjaxModalTarget">
      </div>
  </div>
</div>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Jost:wght@300;400&family=Lustria&display=swap');

  .custom-qv-modal { 
    position: fixed; inset: 0; z-index: 10000; display: none; 
    align-items: center; justify-content: center; background: rgba(0,0,0,0.4);
  }
  .custom-qv-modal.is-active { display: flex; }
  
  .qv-content { 
    background: #fff; 
    width: 311px; 
    height: 447px; 
    padding: 15px;
    position: relative; 
    box-sizing: border-box;
    overflow: hidden;
  }
  
  .qv-close { 
    position: absolute; top: 3px; right: 12px; font-size: 24px; 
    background: none; border: none; cursor: pointer; font-weight: 300; z-index: 10;
  }
  
  .modal-grid { 
    display: grid; 
    grid-template-columns: 120px 1fr; 
    gap: 12px; 
    margin-bottom: 15px; 
  }

.modal-image img {
    width: 120px;
    height: 140px;
    display: block;
    object-fit: cover;
}  
  /* Typography */
  .modal-title { 
    font-family: 'Jost', sans-serif;
    font-weight: 300;
    font-size: 16px;
    line-height: 1.2;
    margin: 0 0 8px 0;
    color: #333;
  }

  .modal-price { 
    font-family: 'Lustria', serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 1.2;
    margin-bottom: 12px;
  }

  .modal-description { 
    font-family: 'Jost', sans-serif;
    font-weight: 300;
    font-size: 14px;
    line-height: 1.1;
    letter-spacing: -0.01em;
    color: #444;
  }
  
  .option-label { 
    display: block; font-family: 'Jost', sans-serif; font-size: 14px; 
    margin-bottom: 8px; font-weight: 400; 
  }

  .color-swatch-wrapper { 
    display: flex; width: 271px; height: 44px; 
    border: 1px solid #000; margin-bottom: 15px;
  }
  .swatch-item { flex: 1; border-right: 1px solid #ccc; display: flex; }
  .swatch-item:last-child { border-right: none; }
  .swatch-item input { display: none; }
  
  .swatch-label { 
    width: 100%; height: 100%; display: flex; align-items: center; 
    padding-left: 12px; font-family: 'Jost', sans-serif; font-size: 16px; cursor: pointer;
  }
  
  .color-indicator { width: 6px; height: 100%; margin-right: 10px; margin-left: -12px; }
  
/* Wrapper for the custom dropdown */
  .custom-select-wrapper {
    display: block; 
    width: 271px; 
    height: 40px; 
    border: 1px solid #000; 
    position: relative;
    cursor: pointer;
    background: #fff;
    font-family: 'Jost', sans-serif;
  }

  /* The Button/Trigger User Clicks */
  .custom-dropdown-trigger {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: flex-start; /* Aligns text to left like input */
    padding-left: 15px;
    position: relative;
  }

  .dropdown-text {
    font-size: 15px;
    color: #000;
  }

  /* The Arrow Image from Assets */
.dropdown-icon-wrapper {
  position: absolute;
  right: 0;
  top: 0;
  height: 100%;
  width: 40px; 
  display: flex;
  align-items: center;
  justify-content: center;
  border-left: 1px solid #000; 
}

.dropdown-arrow-icon {
  pointer-events: none;
  transition: transform 0.3s ease;
  width: 12px;
  height: 6px;
}

.dropdown-text {
  font-size: 15px;
  color: #000;
  padding-right: 45px; 
}
  /* Rotate arrow when open */
  .custom-select-wrapper.is-open .dropdown-arrow-icon {
    transform: translateY(-50%) rotate(180deg);
  }

  /* The Dropdown List (Matches your image: Clean, Centered, White) */
  .custom-dropdown-options {
    position: absolute;
    top: 100%; /* Appears directly below */
    left: -1px; /* Aligns with border */
    width: calc(100% + 2px); /* Matches width including borders */
    background: #fff;
    border: 1px solid #000;
    z-index: 100;
    display: none; /* Hidden by default */
    max-height: 200px;
    overflow-y: auto;
  }

  .custom-select-wrapper.is-open .custom-dropdown-options {
    display: block !important;
  }

  /* Individual Options (XS, S, M) */
  .custom-option-item {
    padding: 12px 0;
    text-align: center; /* Centered like your image */
    font-size: 14px;
    color: #000;
    cursor: pointer;
    transition: background 0.2s;
  }

  .custom-option-item:hover {
    background-color: #f2f2f2;
  }
  
  /* Highlight selected option style */
  .custom-option-item.selected {
    background-color: #000;
    color: #fff;
  }
  .modal-atc-btn {
    width: 271px; background: #000; color: #fff; height: 50px; 
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 20px; border: none; cursor: pointer; margin-top: 20px;
  }
  .atc-arrow { font-size: 24px; font-weight: 200; }

  /* Added Css for Checked swatch */
  .variant-selector:checked + .swatch-label {
    background-color: #000;
    color: #fff;
    transition: all 0.2s ease;
  }

  .swatch-item:hover .swatch-label {
    background-color: #f9f9f9;
  }
  .swatch-item:hover .variant-selector:checked + .swatch-label {
    background-color: #000; 
  }
</style>